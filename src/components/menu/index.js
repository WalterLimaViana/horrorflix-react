import React from 'react';
import Logo from '../../assets/img/Logo.png';

function Menu() {
    return (
        <nav>
            <img src={Logo} alt="horrorflix logo"></img>
        </nav>
    );
}

export default Menu;
